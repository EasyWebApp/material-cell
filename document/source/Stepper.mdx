---
title: Stepper
icon: format_list_numbered
description: Steppers convey progress through numbered steps.
---

import { Stepper } from '../../source/Stepper';

import { Example } from '../../test/component/Example';

Steppers display progress through a sequence of logical and numbered steps. They may also be used for navigation.

## Horizontal steppers

Horizontal steppers are ideal when the contents of one step depend on an earlier step.

<Example>
    <Stepper
        path={[
            { title: 'Name of step 1' },
            { title: 'Name of step 2' },
            { title: 'Name of step 3' }
        ]}
        currentIndex={1}
    />
</Example>

```TSX
import { render, createCell } from 'web-cell';
import { Stepper } from 'material-cell/source/Stepper';

render(
    <Stepper
        path={[
            { title: 'Name of step 1' },
            { title: 'Name of step 2' },
            { title: 'Name of step 3' }
        ]}
        currentIndex={1}
    />
);
```

## Vertical steppers

Vertical steppers are designed for narrow screen sizes. They are ideal for mobile.

<Example>
    <Stepper
        direction="vert"
        path={[
            { title: 'Name of step 1' },
            { title: 'Name of step 2' },
            { title: 'Name of step 3', icon: 'shopping_cart' }
        ]}
        currentIndex={1}
    />
</Example>

```TSX
import { render, createCell } from 'web-cell';
import { Stepper } from 'material-cell/source/Stepper';

render(
    <Stepper
        direction="vert"
        path={[
            { title: 'Name of step 1' },
            { title: 'Name of step 2' },
            { title: 'Name of step 3', icon: 'shopping_cart' }
        ]}
        currentIndex={1}
    />
);
```
